<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardview"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="?attr/cardViewBackground"
    app:cardCornerRadius="5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp">

        <TextView
            android:id="@+id/title_text"
            android:layout_width="0dp"
            android:layout_height="@dimen/image_in_item_height"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:padding="5dp"
            android:textColor="?attr/customTextColor"
            android:textSize="@dimen/item__text_size"
            tools:text="标题"/>

        <ImageView
            android:id="@+id/title_image"
            android:layout_width="@dimen/image_in_item_width"
            android:layout_height="@dimen/image_in_item_height"
            tools:src="@drawable/menu_avatar"/>
    </LinearLayout>

</android.support.v7.widget.CardView>